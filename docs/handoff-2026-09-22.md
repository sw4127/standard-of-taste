# Session handoff — E21 (work dated 2026-09-16, written 2026-09-22)

**Read this first, then `docs/rt-answers-2026-09-16.md`, then `CLAUDE.md`, then
`docs/slice-protocol.md` and `docs/redteam-protocol.md`.** Supersedes
`docs/handoff-2026-09-13.md` (E20) for current state. `docs/queue-of-record.md` remains the
single place every track is accounted for.

**Closing trigger: (6) the PM said to wrap up.** Context was not near exhaustion; the approved
work — Tracks U and T, both complete — was finished, and the owner asked for the handover.

**2258 tests in 185 files, green · tsc clean · 0 lint errors (20 pre-existing warnings, count
unchanged before and after) · 14 commits, ALL PUSHED AND VERIFIED.** `origin/main == local main
== 67c5113`, confirmed by `git ls-remote`.

---

## WHAT THIS SESSION WAS

A product-direction change, not a continuation of the Phase 3 queue. The brief is
`docs/mrd-prompt-card-2026-09-16.md`, now tracked. Its argument in one line:

> **The product ends in a threshold in cents. That number is EVIDENCE, and it had been
> standing in the position of the DELIVERABLE for three months — which is why a technically
> sound instrument is neither enjoyable to use nor convincing to look at.**

Two tracks shipped. **Track U** put the 2026-09-16 rulings into the record and amended the
constitution. **Track T** built the missing terminal surface: a prompt card that turns a
measured threshold into words a text-to-music generator accepts. **No fifth instrument was
added and none was needed.**

---

## WHAT SHIPPED

### Track U — the ruling record (S1–S5, plus RT-Z11)

| Commit | What |
|---|---|
| `9e40456` | `docs/rt-answers-2026-09-16.md` — six rulings, tracked, with reasoning |
| `2e62b1f` | The MRD tracked; 13 market claims labelled **in place** |
| `8023847` | `mrd-labels.test.ts` — the EVIDENCED/ASSUMED ratio is recounted, never typed |
| `0b0e4b3` | **The D1 amendment**, scoped; the memo stamped; the price held by a test |
| `7a2e0c1` | `/method`'s **first reversal** — its own ledger, its own heading |
| `8388708` | RT-Z11 (a): the constitution withdraws its characterisation of the owner |

**The D1 amendment is the constitutional event of this session.** D1 — *the product describes
what you did, never what you are* — is now **suspended for the prompt card and for nothing
else** (RT-Z5 b, RT-Z9 a). Every instrument readout keeps D1 intact. The register that
replaced it is **offer, do not assert**. The carve-out (RT-Z10 a) is that no surface,
including the card, asserts anything about trauma, abuse or mental health. Full text in
`CLAUDE.md` under "D1 amendment"; the old D1 is stamped and readable in the memo.

### Track T — the prompt card (T-S1…T-S6)

| Commit | What |
|---|---|
| `f2de179` | The band measurement — how finely a ladder can say anyone discriminates |
| `52a7aa3` | `src/engine/prompt-card.ts` — structure computed, model nowhere in the path |
| `57291a4` | `src/content/card/` — the sentences, assembled, four rules with planted specimens |
| `80db6a3` | `PromptCardPanel` above the threshold figure; expert view untouched (RT-Z7 b) |
| `c59ae20` | Clipboard, and the on-surface statement **derived from the amendment** |
| `14e81a3` | The chrome joins the scans; the surface accounts for every word it renders |

What a real card says, read from the rendered page:

```
What your ear separates
  Pitch drift is something you hear finely. You were still calling it at 12.5 cents.
What that is worth in a prompt
  Spend words on tuning character and pitch stability. You will hear whether they were obeyed.
Paste this
  clean intonation, stable pitch across the take
```

### The close-out (2026-09-22)

| Commit | What |
|---|---|
| `3b54d28` | This handoff |
| `67c5113` | Batch 6 — the card's copy commissioned; the writing brief corrected; `/method`'s reversal wired into the deck; **`.githooks/pre-commit`** |

**`docs/commission-batch-6.md` (PM ruling RT-AB1 a)** commissions the card's 29 strings as a
document, in the shape batch 5 set, with the file and export name beside each string.
`commission-note.test.ts` pins every one to the modules — the fixed strings verbatim, and the ten
templated branches by rendering each from a real axis and checking every fixed fragment survives in
the note. Rewording a branch or a vocabulary tag without following it in the note fails.

**`.githooks/pre-commit` (PM ruling RT-AB3 a)** refuses to commit any file on the
untracked-by-ruling list, and `githooks.test.ts` pins that list so it cannot shrink in silence. It
exists because of finding 12 below.

---

## FINDINGS THAT MUST NOT BE LOST

1. **THE COMPRESSION LADDER SUPPORTS ONE BAND. It can say a threshold and nothing about how
   finely it was heard.** Pitch and timing support two. I shipped `CARD_BANDS` as a guess of
   2/2/2 *before* measuring and the first run refuted it. `docs/analytics/e21-card-bands.txt`.
   Any future copy implying a fineness comparison on compression is false.
2. **"NOT ENOUGH TO TELL" IS THE CARD'S COMMON OUTPUT, NOT ITS EDGE CASE.** Across listeners
   drawn over a ladder's whole range, a point estimate comes back in **80%** of pitch sittings,
   **30%** of timing, and **16%** on one compression source. A listener at the floor or ceiling
   cannot be bracketed and the instrument says so. The card is built for that answer.
3. **A TEST WRITTEN RELATIVE TO ITS OWN CONSTANT CANNOT DETECT THAT CONSTANT BEING ZEROED.**
   My sample-floor specimen was `MIN_REPEAT_N - 1`, so setting the floor to 0 moved the
   specimen with it and the test passed with the guard switched off. This is the scale-free
   vacuity `arc-resolution.test.ts` documents catching in its own criterion (iii) — reproduced
   one file later. **Specimens must be absolute numbers.**
4. **`bandFor` READ THE COMPRESSION LADDER BACKWARDS AND NOTHING COULD SEE IT.** Rungs there
   are bitrates and the ladder runs *downward*: 32 kbps is the harshest damage and the coarser
   ear. Ordering by the smaller number called that ear *fine*. It was invisible through
   `bandFor` because compression has one band, so every index is 0 either way. **A defect that
   only appears when a measurement improves is the worst kind to leave in** — fixed by
   extracting `bandIndexIn` and exercising a two-band down ladder directly.
5. **THE CARD AND THE SCREEN DESCRIBED ONE SITTING WITH TWO DIFFERENT NUMBERS.** The card said
   "7.5 cents" directly above a headline reading "12.5 cents" — the fitted threshold and the
   rung actually caught, both true, irreconcilable to anyone looking at them together. Found
   by reading the rendered page. `thresholdCardFigure` is the product's one canonical figure
   and every surface must take it.
6. **`tsc` CAUGHT WHAT THE GREEN RUN COULD NOT.** My own fixtures called a four-argument helper
   with three; the observer was built at `magnitudes[undefined]` and every assertion passed
   against a degenerate session. Vitest does not typecheck. **A green suite is not the
   evidence on its own.**
7. **THE CLIPBOARD API'S FAILURE PATH IS THE COMMON ONE.** `writeText` rejects with
   `NotAllowedError` whenever the document is unfocused — every embedded preview, every
   automated run, any tab the reader clicked away from. The first handler swallowed it
   silently. It now selects the text and relabels the button.
8. **THE `RT-Z` ID NAMESPACE WAS ALREADY EXHAUSTED.** RT-Z2, Z6, Z7, Z8 and Z9 each carried an
   unrelated earlier answer before this session reissued them. Rulings are **date-qualified**
   from here; the collision table is §0 of `docs/rt-answers-2026-09-16.md`.
9. **FORTY MINUTES WENT INTO A 404 THAT WAS A STALE `.next` CACHE.** `/threshold/<slug>/result`
   returned 404 for a valid payload while the same code path rendered fine in Node. `rm -rf
   .next` fixed it. I nearly filed it as a SHIP-RISK about broken share permalinks.

10. **THE WRITING BRIEF TOLD COWORK D1 IS ABSOLUTE.** `docs/copy-commission.md` — the first file
    any writing pass reads — still said *"every sentence is about the performance, never about the
    person"*, which stopped being true on 2026-09-16. A writer handed it would have been forbidden
    from doing the one thing the prompt card is **required** to do. The brief is **generated**, so
    the fix is in `scripts/export-commission.mjs`; a guard checks the OUTPUT, and checks the
    boundary too, because *"D1 no longer applies"* is a more dangerous sentence than the rule it
    replaces.
11. **A GENERATED ARTEFACT IS ONLY AS FRESH AS THE LAST RUN OF ITS GENERATOR.** The committed copy
    deck said **"The six refusals"** over a page rendering **seven**, for nine days, and nothing
    failed — the seventh shipped on 2026-09-13 and nobody re-ran the exporter. Worse, `/method`'s
    **reversal was in no deck at all**: the newest block on the page, invisible to the copy deck,
    the review ledger, and any writer handed either. **A surface that renders is not the same as a
    surface the copy system can see.** Both are fixed; the drift canary in
    `scripts/deck-templates.test.ts` is what caught the first.
12. **`git add -A docs` STAGED SEVEN FILES THAT WERE DELIBERATELY UNTRACKED**, one message after
    asking the owner's permission to track exactly one. It was committed. **Nothing was pushed**,
    the owner ruled it undone (RT-AB2 a), and `git reset --soft HEAD~1` restored them. The suite was
    green throughout — 2254 tests, tsc clean — and `git status` was the only thing that would have
    shown it. The rule it broke was a sentence in a prompt, read and agreed with the same day.
    **Stage files by name. Never `git add -A` or `git add <dir>`.**

---

## STILL UNVERIFIED — carried forward, do not quietly empty this list

- **THE PROMPT CARD'S COPY HAS NEVER BEEN THROUGH A WRITING PASS.** It is *gate-clean*, which
  the voice gate's own docblock says is not the same as good. Twenty-nine strings. Two I would
  not defend in review: *"That is a fact about this sitting, not a limit"* and *"Nothing here
  says a finer request would be worth the words"*. **This is the largest gap between what
  shipped and what a reader would call finished.**
- **The clipboard SUCCESS path has never been observed.** This automation profile has
  clipboard-write permission DENIED, so every run takes the fallback. The fallback is verified
  end to end; `writeText` succeeding is not.
- **The flow's last screen was not walked.** The card is verified on the share permalink, which
  renders the same component. Nobody has sat 26 audio trials and seen the card arrive.
- **The tag vocabulary is invented by engineering.** No generator was consulted and no prompt
  produced by this card has ever been pasted anywhere to see whether the words do anything.
- **All band figures are SIMULATED**, n = 0 real respondents, and the listener model is the
  same logistic the fitter assumes — which flatters the fitter.
- **Band boundaries are equal in log magnitude.** A defensible default and nothing more; they
  are not claims about which detunings matter musically. Saying where they *should* fall needs
  listeners this project does not have.
- **The copy deck was regenerated wholesale and not read line by line.** The guards cover the
  counts and the ids, not every sentence in `docs/copy-deck.md` or the review ledger.
- **Batch 6 has been handed to nobody**, and the card's strings are still **not in the deck** — it
  is commissioned as a document because wiring `src/content/card/` into the vocabulary-layer
  machinery is unruled work nobody has done.
- Carried from E11–E20 and still true: audio audibly playing, any event or `?ref=` reaching a
  sink, and `docs/index.html` rendered by anyone.

---

## OPEN WORK, IN PRIORITY ORDER

1. **The consistency close (Track V).** The next session's task. The activation prompt was
   emitted in chat on 2026-09-22 and merges the owner's original with nine additions, three of
   them from findings 10–12 above; `docs/activation-2026-09-22-track-V-consistency.md` on this
   machine is the owner's ORIGINAL and does not carry them. One known defect: `/learn` is reachable twice from the front door's viewport, as the header nav
   item *"READING ROOM"* and as the secondary door *"Reading room…"*.
   **Evidence gathered this session, so the next one does not start from zero:**
   `SECONDARY_DOORS` lives in `src/content/landing.ts:168` **as data**, gated by the voice
   deck; `HEADER_LINKS` is an inline literal at `src/app/page.tsx:160–164` and
   **`git grep` finds no test that reads `HEADER_LINKS` or `SiteHeader`**. That points at (a)
   or (b) rather than (c) — but it is a source scan, and the next session is required to answer
   it against rendered output.
2. **Hand batch 6 to Cowork.** `docs/commission-batch-6.md` is written, tracked and pinned to the
   modules. **It is the largest gap between what shipped and what a reader would call finished.**
   Batches 4 (`/method`) and 5 (README, pitch page, front door) are still written and unhanded
   too; batch 4's stated counts were corrected this session and are now held by a test.
3. **Track O — the first real number.** OPEN and blocked on the owner: sit all four
   instruments so one result exists badged REAL, n = 1. Two Tier 1 goals in `docs/kpis.md`
   wait on it, and so does Track P (the retest arc, which then waits seven days).
4. **Owner actions, carried:** the GitHub Support ticket is submitted and the reply unread —
   **that reply is what releases the four backup refs (one branch, three tags), and until then
   never `git push --force --mirror origin`**; the engine package is recommended CUT and not to
   be done without a fresh ruling.

---

## PM RULINGS THIS SESSION

**RT-Z5 (b)** the readable output may speak about the person · **RT-Z6 (a)** the MRD is tracked
and labelled · **RT-Z7 (b)** the card sits beside the threshold readout, card above · **RT-Z8
(a)** generic wording, no generator named · **RT-Z9 (a)** D1 suspended for the card alone ·
**RT-Z10 (a)** the trauma/abuse/mental-health carve-out stands · **RT-Z11 (a)** the constitution
withdraws its characterisation of the owner · **RT-Z12 (a)** the card shows only the axes
actually measured.

**RT-AB1 (a)** write the card's commission as a tracked file rather than leaving it in chat ·
**RT-AB2 (a)** undo the accidental tracking of seven ruled-untracked files · **RT-AB3 (a)** add a
pre-commit hook that refuses them.

Engineering decided and reported rather than asking: date-qualifying the RT-Z ids; reading
RT-Z8's scope as binding the product surfaces and not the evidence documents; emitting the
reversal as its own deck section rather than appending it to the refusals, which would have
changed the count in the heading above it.

**Nothing is awaiting a ruling.** The decisions block was empty at the close.

---

## Files this session touched

**New:** `docs/rt-answers-2026-09-16.md` · `docs/analytics/e21-card-bands.txt` ·
`src/engine/card-bands.ts` + `prompt-card.ts` (+ tests) · `src/analytics/card-bands.test.ts` ·
`src/content/card/{axes,copy,statement}.ts` (+ tests) · `src/content/mrd-labels.test.ts` ·
`src/content/d1-amendment.test.ts` · `src/components/PromptCardPanel.tsx` +
`PromptCardPanel.test.tsx` + `card-surface.test.tsx` · `src/lib/card-history.ts`

Added in the close-out: `docs/commission-batch-6.md` · `docs/handoff-2026-09-22.md` ·
**`.githooks/pre-commit`**

**Changed:** `CLAUDE.md` (D1 amendment, Roles stamp, on-surface statement) ·
`scripts/export-commission.mjs` + `export-method-deck.mjs` + `deck-templates.test.ts` +
`githooks.test.ts` · `docs/commission-batch-4.md` · the generated decks, ledger and brief ·
`restructuring_decision_memo_2026-07-11.md` (D1 stamped — its first ever) ·
`docs/mrd-prompt-card-2026-09-16.md` (now tracked) · `docs/queue-of-record.md` ·
`src/content/method/claims.ts` + `claims.test.ts` · `src/app/method/page.tsx` +
`inference-mark.test.ts` · `src/content/retired-gates.test.ts` ·
`src/content/queue-of-record.test.ts` · `src/app/threshold/ThresholdResult.tsx`

Still untracked, each for its own reason: `docs/blueprint-phase-2.md`,
`docs/blueprint-phase-3.md`, `docs/redirection-blueprint-2026-08-26.md`, `.claude/`,
`fan-cards/`, `promo-assets/`. **The repository is public: tracking a file publishes it. Ask
before adding any of them.**
