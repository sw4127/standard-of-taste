# Session handoff — 2026-08-28b (E11: Track B, and the publication attempt)

**Read this first, then `docs/redirection-blueprint-2026-08-26.md`, then `CLAUDE.md`, then
`docs/slice-protocol.md` and `docs/redteam-protocol.md`.** Supersedes `docs/handoff-2026-08-28.md`
(E10).

**Closing trigger: (6) — the PM said to write the handoff.** The approved queue is also finished:
Track B is closed, RT-AR is shipped, and the only remaining item (RT-AV) the PM has explicitly
deferred to the very end of all tracks.

**2033 tests green · tsc clean · eslint 0 errors (15 pre-existing warnings) · `next build` green ·
7 commits.**

---

## ⚠ THE PUBLICATION IS DONE — WITH ONE RESIDUAL EXPOSURE THE OWNER MUST CLOSE

### What the exposure was, and that it is now fixed on the default branch

`github.com/sw4127/standard-of-taste` is **PUBLIC** (verified with `gh repo view`). Its `main` was
the **pre-rewrite** history, carrying two personal email addresses in commit metadata that the PII
rewrite had removed locally and never pushed. Verified by comparing masked author/committer sets:

| | distinct author/committer addresses |
|---|---|
| local `main` | 3 — all `noreply` / `.local` |
| `origin/main` BEFORE | **5** — the same 3, plus `bo***@***.com` and `sw***@***.edu` |
| `origin/main` AFTER (verified 2026-08-28) | **3 — all `noreply` / `.local`** |

**COMPLETED AND VERIFIED:**

```
remote main == local main       4c0d02a, all 340 commits
feat/world-cup-2026-refresh     deleted from the remote
public default branch PII       gone
```

It was remediation, not tidying, and the remediation landed.

### How it was pushed, and why it took nine attempts — read this before any future large push

| Attempt | Result |
|---|---|
| 1 (foreground, 5 min) | timed out — `credential.helper = manager` opens a Windows GUI dialog that cannot be answered with no stdin |
| 2 (foreground, 10 min, `gh` as one-shot helper) | timed out — the tool's ceiling, not a git failure |
| 3 (background, `\| tail -20`) | **reported exit 0 and had FAILED.** The pipeline returned `tail`'s status, not git's |
| 4 (background, `http.postBuffer=734003200`, `http.version=HTTP/1.1`) | **failed.** The buffer fix worked — all 200 MiB buffered at 478 MiB/s — but the send was reset: `curl 55 Send failure: Connection was reset` |
| 5–9 (staged, one chunk at a time) | **SUCCEEDED.** Chunks at commits 40 / 100 / 170 / 240 / 300 / 340 |

Two of the staged chunks still failed on the first try and succeeded on a straight retry — one on
`Could not resolve host: github.com` (DNS, transient) and one on the pre-push hook reporting a red
suite when the suite was in fact green (a concurrent `vitest` run; the hook's own command exits 0).
**Retry once before diagnosing anything on this link.**

A third trap: `git ls-remote` run too soon after a background push reads the OLD ref and looks like
failure. Chunk 3 was declared failed on exactly that basis and had actually landed. **Read the
push's own log for the `a1b2c3..d4e5f6` line, then confirm with `ls-remote`.**

Attempt 3's real error, which is the actual root cause:

```
error: unable to rewind rpc post data - try increasing http.postBuffer
error: RPC failed; curl 65 Recv failure: Connection was reset
fatal: the remote end hung up unexpectedly
```

The pack is **200.01 MiB** (3811 objects), and understanding WHY matters for finishing this:
the rewritten history shares **no commit** with the remote, so git's negotiation finds no common
base and re-sends blobs GitHub already has — the audio files are almost certainly the bulk. Uplink
measured at roughly 60–120 KiB/s on the slow stretch, and the link reset twice mid-send. A DNS
failure also appeared once and cleared on its own, so treat single failures as possibly transient
and confirm against `git ls-remote` rather than assuming.

### The approach that worked, and the one to reuse: STAGED PUSHES

A single 200 MiB push did not survive this link, and each failure cost the whole upload. Pushing
the history in chunks fixed it: once the first chunk lands, the remote and local share history, so
git's negotiation works and every later chunk sends only what is genuinely new. Chunk sizes dropped
from 200 MiB to roughly 1000 objects each, and each success is durable.

**It is safe to interrupt. Every commit on local `main` is PII-clean** (all 340 verified), so a
partially-pushed public repo is still remediated — merely incomplete.

The first chunk must carry the lease check; the rest are fast-forwards:

```
git -c credential.helper='!gh auth git-credential' -c http.postBuffer=734003200     -c http.version=HTTP/1.1     push --force-with-lease=main:ab68c178cef17c0db0cd3d1edf0f8a6f579def9b --progress     origin 4b8dc67711b4:refs/heads/main

# then, in order, each a plain fast-forward:
#   ae785594b8bd  297f31358112  6c3798cc1e78  7220b617c337  and finally main
git -c credential.helper='!gh auth git-credential' -c http.postBuffer=734003200     -c http.version=HTTP/1.1 push --progress origin <sha>:refs/heads/main
```

Checkpoints are commits 40 / 100 / 170 / 240 / 300 of 340, oldest-first
(`git rev-list --reverse main`). Regenerate them if the history moves.

**NEVER PIPE A PUSH INTO `tail` OR `head`.** That is how attempt 3 came back green while the remote
was untouched. Read the log file, and verify with `git ls-remote` — the remote is the only fact.

### The exact commands, verified

```
git -c credential.helper='!gh auth git-credential' -c http.postBuffer=734003200 \
    -c http.version=HTTP/1.1 push --force-with-lease --progress origin main

git -c credential.helper='!gh auth git-credential' push origin --delete feat/world-cup-2026-refresh
```

`credential.helper = manager` is the user's global setting and is **not** changed — `gh` is passed
per-invocation with `-c`. `gh auth status`: logged in as `sw4127`, scopes `gist, read:org, repo,
workflow`. `--force-with-lease` compares against our stored `origin/main` (`ab68c17`), which
`git ls-remote` confirmed matches the real remote — so the safety check is meaningful, and it will
refuse if anyone else pushed meanwhile.

The stray branch `feat/world-cup-2026-refresh` (`e385ef9`) is **also pre-rewrite** and carries the
same addresses. Deleting it is part of the remediation, not housekeeping. **It has been deleted —
verified: it no longer appears in `git ls-remote`.**

### ⚠ BUT THE DELETED BRANCH'S COMMIT IS STILL SERVED, AS A PULL-REQUEST REF

`git ls-remote origin` now returns:

```
4b8dc677…  HEAD
4b8dc677…  refs/heads/main
e385ef9…   refs/pull/1/head     <-- the pre-rewrite commit, still reachable
```

**Deleting a branch does not delete the pull request opened from it.** GitHub keeps `refs/pull/N/head`
for the life of the PR, it is fetchable by anyone, and a normal push cannot delete it — the ref is
owned by the PR, not by the branch. So the addresses in `e385ef9` remain publicly reachable even
though the branch is gone.

This was **not** in the original plan and nobody had noticed the PR existed. It does not change what
engineering can do — the remaining options all belong to the owner:

- close **and delete** pull request #1 from the GitHub UI (deleting the PR removes its ref), or
- include it explicitly in the RT-AV Support request.

**Add PR #1 to the RT-AV request. A GC that misses an open PR ref will not remove this commit.**

### The verification that was run, and that should be re-run after any future history change

```
git ls-remote origin                                  # main == local main; stray branch gone
git fetch origin main && git log FETCH_HEAD \
    --format='%ae%n%ce' | sort -u                     # ONLY noreply / .local
```

Result on 2026-08-28: remote `main` `4c0d02a` = local `main`, 340 of 340 commits; three addresses,
all `noreply` / `.local`. **Do not read `origin/main` for this — read `FETCH_HEAD` after an explicit
fetch.** A stale remote-tracking ref will happily show you the answer you had yesterday.

### PM rulings on what happens next

- **RT-AU (b) — DO NOT delete the four backup refs.** They stay until the PM says otherwise. They
  are the last local copy of the pre-rewrite history and therefore of the removed addresses; they
  are also the only way back if the force-push is wrong. One is a BRANCH and three are TAGS:
  `backup-main-pre-rewrite` (branch, `d0197e8`) · `backup/pre-rewrite-2026-08-27` (`d0197e8`) ·
  `backup/stray-claude-branch` (`18fe802`) · `backup/world-cup-branch` (`e385ef9`).
- **RT-AV (a), DEFERRED BY THE PM to the very end of all tracks and tasks.** A force-push does not
  purge anything from GitHub: the old commits stop being reachable from the branch, the commit
  list, blame and the contributor graph, but GitHub keeps unreachable objects and still serves them
  at direct SHA URLs (`/commit/ab68c17`) until it garbage-collects, and forks or caches keep them
  regardless. **Full removal requires GitHub Support running GC on the repository, or deleting and
  recreating it.** The PM has ruled to ask Support, and to do it last. Engineering cannot open that
  request — it needs the owner's account. **This is the final task in the queue; do not let it fall
  off the end.**

### The pre-publication sweep, run 2026-08-28 — clean

Re-run it if history changes; do not inherit this.

- Key-shaped secrets (`sk-`, `ghp_`, `github_pat_`, `AKIA`, `xox[baprs]-`, PEM, `phc_`) in tracked
  files: **none**.
- Only `.env.example` has ever been tracked.
- **All 340 commits** of local `main`, tracked content: **zero** non-noreply addresses.
- `docs/redirection-blueprint-2026-08-26.md` still untracked, per RT-M(c) / RT-X(c). It must stay
  untracked. So are `.claude/`, `claude_code_kickoff_package.md`, `docs/blueprint-phase-2.md`,
  `docs/index.html`, `fan-cards/`, `promo-assets/`.

---

## What shipped (7 commits)

| Commit | What |
|---|---|
| `aeaf158` | S1 — the flaw families get one source of truth, and the fourth one never existed |
| `36a9a2f` | S2 — the reading room and front door describe the product that shipped |
| `0ad3633` | S3 — `/learn/flaws`, a reference page for the person whose render is broken |
| `0929b66` | S4 — the front door offers a way to name the damage |
| `85e6b7b` | S5 — a result that names a flaw offers somewhere to go with it |
| `8255d88` | S6 — the copy reaches the writing pass; AI readers reach the new page |
| `4c0d02a` | S7 — the gym's surfaces stop wearing the Prestige Test's gold |

New files: `src/content/flaw-families.ts` + `.test.ts` · `src/content/landing.ts` ·
`src/content/instrument-state.test.ts` · `src/app/learn/flaws/page.tsx` ·
two `__fixtures__/*.txt`.

**Track B is closed** against the ruled scope (B1, B2, B3, F1). It is **not** closed against the
blueprint's written definition of done — that sentence says a creator on the front door can name
"the four flaw families", and both halves were superseded before the work started: RT-AO(a)
confirmed RT-C(b) governs (landing stays general), and there are **three** families, not four.

---

## PM rulings this session

RT-AM **(a)** — take Track B · RT-AO **(a)** — RT-C(b) governs the front door, not the DoD sentence ·
RT-AP **(a)** — widen Track B to fix the false statements found in the copy it touches ·
RT-AQ **(a)** — stop at every slice · RT-AR **(a)** — fix the gym-level gold, as its own slice after
Track B · RT-AS **(c)** — run the publication now rather than leaving it to another session ·
RT-AT **(a)** — allow background execution for the push · RT-AU **(b)** — keep the backup refs ·
RT-AV **(a)** — ask GitHub Support to purge, **queued at the very end of all tracks**.

---

## FINDINGS THAT MUST NOT BE LOST

1. **A PIPED COMMAND REPORTS THE PIPE'S EXIT CODE.** `git push … | tail -20` returned **0** while
   the push had failed outright and the remote was untouched. Every previous finding in this
   repository about guards that only ever say "clean" applies to shell pipelines too. For anything
   that writes to a remote: do not pipe, and verify against the remote itself.

2. **THE PRODUCT NAMED A FOURTH FLAW FAMILY THAT HAS NEVER EXISTED.** Four places said the trials
   introduce a buried incorrect pitch; `DEGRADATION_FAMILIES` has held exactly three for the life
   of the pool. Two were live pages, one of them an FAQ answer also served as FAQPage JSON-LD, so
   search engines were told it too.

3. **THE READING ROOM ADVERTISED AN OPEN MACHINE AS LOCKED FOR TWENTY DAYS.** Delicacy opened
   2026-08-08; the index card said "The locked machine", an FAQ asked "When do the Delicacy Trials
   open?", and the front door said "Two machines" over three cards. All were true when written and
   nothing tied them to the flag that stopped them being true.

4. **THE GUARD MUST READ THE FLAG, NOT SWEEP FOR THE WORD.** Two blocks legitimately say "locked" —
   the D3 visible-and-locked door on the front door and the bias debrief, both inside
   `!DELICACY_LIVE`. A text sweep cannot tell them from the four defects, and the repair it pushes
   you toward would delete the door. The assertion is the biconditional: state language appears iff
   the instrument is not live.

5. **MY BICONDITIONAL WAS TOO STRONG AND ONLY PROVING IT CAUGHT THAT.** The first version demanded
   every delicacy string carry locked language when shut — including the page title. The two
   directions are not symmetric. *Assert a guard's scope; prove it in both directions.*

6. **THREE MORE NEEDLES WERE WEAKER THAN THEIR NAMES**, found only by breaking them:
   `toContain("MACHINE 02")` passed while the locked marker was stripped · `toContain("FLAWS_LIMITS")`
   passed while the block was deleted, because the **import line** still contained the name ·
   `toContain("DELICACY_LIVE ?")` asserted a code style and broke on reformatting. Assert the
   **braced/rendered form**, not the bare identifier.

7. **RENDERED OUTPUT BEAT GREP FOUR TIMES.** "pick either" under three machine cards — it counts to
   two without using a number, so every numeric and state sweep was blind to it · "machine 02 , and
   they are open", a stray space from a JSX line break · "Measured in kbps (kbps)", because lossy's
   short and full unit are identical · a lowercase sentence opening, "Not a vibe. three machines".
   **Read the built HTML and the running DOM. The source does not tell you what ships.**

8. **NOTHING MAY COUNT.** The invite from a result to the reference first read "The other two
   flaws" — true after a Threshold session (one family), false after a Delicacy one (all three).
   Arity in a sentence reused where the arity differs is the same defect as "pick either". Guards
   now refuse counting words in both strings.

9. **THE BACKSLASH TRANSPORT EATS ONE LEVEL, AND IT BIT FOUR TIMES.** `\\n` arrived as a real
   newline (unterminated string), `\\b` as a literal **0x08 BACKSPACE** that renders as nothing and
   silently turned `/\btwo\b/i` into `/two/i`, and `\\1` as a swallowed backreference. Write **no
   escapes at all** in scripted content: `String.fromCharCode(10)`, `indexOf`, word-membership sets.
   Sweep with `grep -rlP '\x08|\x01' src/`.

10. **`git checkout --` DESTROYS UNCOMMITTED WORK.** Used to undo a temporary proof edit, it
    reverted a file to HEAD and wiped that slice's real change. Use `cp` to a backup and restore
    from that. (The guard I had just written caught it, which is the only reason it was noticed.)

11. **A RULING IS NOT A RULE UNTIL SOMETHING FAILS WHEN IT IS BROKEN.** RT-AG made the gym neutral
    on 2026-08-27; E10/S6a implemented it and its commit asserted "the only colour left is the
    three machine cards themselves". **Eleven sites across five files still painted gold.** The
    ruling had been applied to the fields and the wordmark, not the text.

12. **REMOVING A HUE MOVES ITS JOB SOMEWHERE.** The gold link measured 5.56:1 against the front
    door's worst backdrop while `--muted` body copy measures 4.67:1 — a separation of **0.89**. The
    link was told apart from its paragraph almost entirely *by being gold*. `GYM_INK` measures
    8.50:1 / 9.38:1, **3.83** clear of body copy, and within 0.13 of the 8.37:1 the gold headings
    already had — so hierarchy is preserved while links gain the contrast the hue used to supply.

13. **`tsc` CAUGHT WHAT 2000+ GREEN TESTS DID NOT**, twice more: a `Set<DegradationFamily>` probed
    with a `string`, and a missing import that failed at runtime with an opaque trace.

14. **B3 WAS ALREADY BUILT.** Track A shipped the creator language on all three result screens in
    E8. Checking before building is why S5 became "give it somewhere to go" instead of a rewrite.

15. **I NEARLY REPORTED A FIFTH FALSE FINDING.** A narrow query returned `goldFound: []` on
    `/bias`, which reads as "the instrument lost its colour". The needle only inspected `color`;
    that screen's gold is a **border**. Broad re-query found it. A narrow needle proving an
    *absence* is the trap this environment sets over and over.

---

## STILL UNVERIFIED (do not claim otherwise)

- **Nothing was seen as pixels.** Every appearance claim is a computed style, built HTML, or a DOM
  read. The browser pane runs hidden here: `requestAnimationFrame` never fires, CSS transitions
  never advance (set `transition:'none'`, read, restore), screenshots time out. Always re-check in
  a **newly created tab** — HMR-buffered console errors survive navigation.
- **No hover state has been observed anywhere.** `GYM_INK_BRIGHT` has never been seen by anyone.
- **Nobody has looked at the four gym surfaces since they were recoloured** — `/learn`, `/lab` and
  `/method` have now gone neutral unseen twice.
- **The shut state of the reading room has never been rendered.** `DELICACY_LIVE` was flipped only
  inside test runs.
- The three creator vocabularies are guarded for **coverage**, not consistency: nothing stops them
  describing the same family in different words, and a reader who sees two result screens will meet
  the same flaw described twice.
- The deck guard covers **only E11's strings**. Batches 1–4 can still drift from their deck entries.
- The "no causal promise" guard is a five-phrase list. A sixth phrasing passes.
- `~8 min` is still hardcoded in three files, derived from nothing, deliberately unguarded.
- E10's list still stands: audio audibly playing, the 8-second gate against a real clock, the ring
  animating, the expert panel's `<details>` clicked, any event or `?ref=` value reaching a sink.

---

## Open work, in priority order

1. **PULL REQUEST #1 — the last piece of the exposure, and only the owner can close it.**
   `refs/pull/1/head` still serves `e385ef9`, the pre-rewrite commit carrying both removed
   addresses. Deleting the branch did not touch it. Close **and delete** PR #1 in the GitHub UI, or
   name it explicitly in the RT-AV request. **A GC that misses an open PR ref will not remove this
   commit.** This is a two-minute owner action and it is the only part of the publication still
   outstanding.
2. **Track B's copy is with the PM for its writing pass** — `docs/copy-deck-instruments.md` batch 5
   now carries all of E11's strings, generated. Re-run
   `node scripts/export-instrument-deck.mjs > docs/copy-deck-instruments.md` after any copy change;
   a guard fails and names that command if you forget. `docs/copy-deck-vocabulary.md` and
   `-method.md` are still out too.
3. **RT-AV — GitHub Support purge request. PM-ruled, deliberately LAST, after every other track and
   task.** Engineering drafts it; the owner sends it.
4. Deferred with reasons:
   - Reading-room prose links are not `Jump`s, so "if it moves you, it is mono" does not hold in
     `Explainer`. Achromatic links lost the hue affordance and gained only brightness and weight.
     Pre-existing; not touched in a copy/colour slice.
   - `ICE = THRESHOLD_VIOLET` in `ThresholdResult.tsx` — a stale variable name from before the
     Threshold Test had its own accent. The rendered colour is correct.
   - `GymFloor` and `/threshold` still build machine cards from separate registries — a fifth list
     of machines. Still not investigated.
   - The unit labels on `/learn/flaws` (`cents of peak detune`, `ms of drift IQR`) are the
     pipeline's own names on a page whose premise is plain language. Flagged in the deck as the
     weakest lines, with the reason they were kept.
