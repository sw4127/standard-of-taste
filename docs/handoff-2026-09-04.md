# Session handoff — 2026-09-04 (E16: Track I, the Comparison instrument)

**Read this first, then `docs/blueprint-phase-2.md`, then `CLAUDE.md`, then `docs/slice-protocol.md`
and `docs/redteam-protocol.md`.** Supersedes `docs/handoff-2026-09-02b.md` (E15) for current state.
`docs/handoff-2026-08-28b.md` (E11) remains the reference for the push mechanics and the backup-ref
commands.

**This document is written mid-session and will be refreshed at the close.** It exists now because
the falsified registry's completeness guard parses the FALSIFIED sections below out of `docs/`, and
an entry on `/lab/falsified` that rests on no record is precisely what that guard exists to refuse.

---

## TRACK I IS BUILT. HUME'S FIFTH CRITERION HAS AN INSTRUMENT, AND IT COST NO NEW AUDIO.

**Breadth means DEGREES USED (PM ruling RT-H b)** — how many of the eleven points on the rating
scale a listener actually landed on, computed from the blind pass the Prestige Test already collects
and already stores. No clip, no tap, no fourth machine.

**The connection to modern criticism is their SCALE, not their catalogue (PM ruling RT-I3 a).**
Pitchfork's records can never be played here, so the instrument borrows the thing that is actually
comparable: what professional critics do with their own degrees. Pitchfork allows a hundred and one
places to put a record and puts most of them between 6.4 and 7.8; Christgau graded A+ to E− and from
1990 stopped using most of the letters below B+.

### PM rulings this session

- **RT-I1 (c)** — extend the pool with critic-ranked works rather than shipping degrees alone.
  Superseded within the session by RT-I3 once the sourcing was researched.
- **RT-I2 (a)** — Comparison as a separate return-visit instrument, front door untouched.
  Rendered moot by RT-I3(a): with no new audio there is no separate session to sit.
- **RT-I3 (a)** — **no new audio.** Comparison is a reading on the Prestige result; the critic-scale
  reference is the modern-criticism connection; the Beethoven audio half becomes its own track.

---

## FALSIFIED — do not re-derive

1. **"A published critics' ranking can set the spread over the clips the Prestige Test already plays."**
   Checked before building, against the sources rather than from memory. The pool is CC0/public-domain
   repertoire chosen for licence clarity, and **no enumerated critic ranking covers it**: Michael
   Tanner's ranked twenty-one Beethoven works (BBC Music Magazine) contains none of our works, and
   **four of the fourteen scored clips are Creative Commons artists no critic has ever ranked**. The
   consequence is RT-I3(a): critics supply the reference SCALE, never the spread over our items.
2. **"The BBC Music Magazine list of fifteen Chopin works is a ranking we can use for spread."**
   It would have been ideal — short piano works where a twenty-second excerpt is a fair sample, all
   available from a CC0 complete-works set we already licence, one performer throughout. Opening the
   article's own framing killed it: **it is numbered but not ordered by merit**, with no countdown
   and no statement of greatness. Using its numbers as a spread would have been inventing a ranking.
3. **"Eleven of eleven degrees is a meaningful ceiling for the degrees-used count."**
   Rating sixteen clips at random lands on **8.606 distinct values of eleven** — the top of the scale
   is reachable by indifference, and two simulated listeners with entirely different stability both
   scored 11 of 11. The engine now carries the null model and no surface may print the count without
   it.

---

## Open work, in priority order

1. **Track I remains open at S7** — the Lab slice. Refresh this document at the close.
2. **TRACK P — the professional reference point, extended.** Analysis written at the PM's request:
   `docs/professional-reference-analysis-2026-09-04.md`. It generalises, it is five slices, and one
   of them can fail. The rule that makes it safe is narrow — **a citation may describe the measuring
   apparatus, never how well people score** — and the cheapest slice needs no external sourcing at
   all, because the clip pipeline already uses EBU R128 and a 320 kbps transparency anchor and tells
   the reader neither.
3. **The Beethoven audio half — a new track.** RT-I3(a) parked it: critic-ranked works the reader
   actually HEARS, gated on whether public-domain recordings exist for enough of Tanner's twenty-one.
   That sourcing question is answered first and cheaply, before anything is built.
4. **The residual exposure.** `refs/pull/1/head` still absent from the remote; Support has not
   confirmed in writing. **RT-L8 (a): the four backup refs stay until they do.** One is a BRANCH
   (`backup-main-pre-rewrite`), three are TAGS. **NEVER `git push --force --mirror origin`.**
5. **The writing pass on the three copy decks** — the PM's, not engineering's, and this session added
   copy to it: the comparison reading, three new FAQ answers, and a rewritten reading-room page.
6. **Owner actions:** enable GitHub Pages (`main` → `/docs`) · create and flip the engine-package
   repository · the GitHub Support ticket.
