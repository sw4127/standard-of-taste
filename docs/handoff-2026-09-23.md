# Handoff — 2026-09-23 (Track V close + Track W, the snack's return)

**Closing trigger:** (1) context near exhaustion, and (6) the PM said to wrap up.

## What shipped since `docs/handoff-2026-09-22.md`

| Commit | Slice | Ruling |
|---|---|---|
| `739ba60` | W-S1: the constitution names the snack as a second D1 surface; the card's statement narrows to "in the gym" | RT-5 (a), RT-6 (a) |
| `9015554` | W-S2: `/music/quiz` + `/music/result` restored as a named D1 surface, no paywall; `ResearchPanel` (P1–P4 + ∴) back on `/music/result` | RT-4 (c) |
| `71b672a` | W-S3: the MRD gains §11 (the insight it left out, M14–M17) and pre-registers the reading build R1–R4 | RT-4 (c) |
| `5fac7fb` | W-S4: the snack keeps its playful verdict voice, exempt from the offer register; `/legal`, `/method` and CLAUDE.md agree | RT-7 (b) |

Earlier in the same session (Track V): RT-1 (a) duplicate "Reading room." door dropped · RT-2 (a) legacy personality
routes retired to `redirect("/")` · RT-3 (a) the slice latch reads the standing IN FORCE heading. The consistency
close is `docs/consistency-close-2026-09-22.md`. Suite at `5fac7fb`: **2,312 tests green, tsc clean.**

## Findings that must not be lost

1. **The thesis is not in the reading room and never was.** The P1–P4 argument with ∴ was added in `49d18d3`
   (2026-06-19) inside `ResearchPanel`, on the legacy music result page. S9 (`2696f67`) deleted it as dead code with
   that page; W-S2 restored it, but it renders **only on `/music/result`**. `src/content/learn.ts` holds no
   premise/conclusion content, and Hume's argument appears there as prose, not as premises. Recommended: add both
   arguments to `/learn` in premise/conclusion form, with P4's inference marked as an inference (N3). Not started.
2. **Three-way conflict on "current taste" — RULED as RT-8 by engineering on the owner's delegation (2026-09-23).** The conflict was an equivocation: spec §9 uses "current" to mean *recent*, the owner means *held now*; durable taste is held now, so the two are compatible. The owner's claim is a value claim, labelled ASSUMED. Stamped on spec §9; MRD M3 restated and M15 corrected; PRD UC-5's gloss aligned to its own source ("past listening predicts less than present and forming taste"). The original finding, kept for the record:
   - Spec §9 (line 221, approved): *We do NOT assert "only your current taste matters"* — about what taste
     **reveals** (recent → state, durable → trait).
   - Owner, 2026-09-23: only current and future taste has **business value**; past taste does not. That is a value
     claim, not a claim about what taste reveals, and it is compatible with §9 once the two are kept apart.
   - `docs/prd-1-use-cases.md` UC-5 labels "present and forming taste matters more than listening history" as
     **EVIDENCED**. It is the owner's position, so the label should be ASSUMED unless a source is cited (N3).
   - `docs/mrd-prompt-card-2026-09-16.md` M3 misstates the owner's claim; restate it as the value claim.
3. **The PRD (parts 1–4) never cites spec §9.** Zero references to the thesis, feeling, or P1–P4 across the four
   files; one incidental "the feeling" in part 4. The PRD was derived from the routes in `src/app`, so it describes
   what was built, not what the blueprint asked for. That is the deviation the owner is worried about.
4. **Unverified:** the snack reading has not been rendered locally end to end (it makes a paid LLM call; a key is
   in `.env.local`, and no paid call was approved this session).

## Open work, in priority order

1. Done (RT-8). The single index to the blueprint's copies is `Claude outputs/blueprint-ledger-2026-09-23.md` — untracked, because it quotes the owner's chat words.
2. Put the thesis and Hume's argument into `/learn` in premise/conclusion form, **after** the Cowork audit settles which proposition the thesis is (the ledger's §4: the only written conclusion, ResearchPanel's ∴, concludes about "the person", while the owner's insight concludes about feeling; and the P1–P4 numbering is not in the spec).
3. The reading build R1–R4 (MRD §11), pre-registered, not started.
4. Dead-code cleanup, corrected scope: quiz skins, world-cup content, premium, `/api/checkout`,
   `/api/premium-hook`, `/product-image`. **Not** the music routes (they are the snack now).
5. The product critique — *not convincing, not fun to try* — is not fixed by anything above. It is being taken to
   a separate planning project anchored on the spec (owner's decision, 2026-09-23).

## Decisions carried forward

- **The unmet demand has never been stated.** The owner wrote "(etc)". Put to the owner by the Cowork audit; not to be filled in by engineering.
- **Whether to track the blueprint ledger** (publishing the owner's chat words).
