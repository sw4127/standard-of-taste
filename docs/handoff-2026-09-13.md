# Session handoff — 2026-09-13 (E20)

**Closing trigger: (1) context, flagged by the PM.** The approved work is also finished — everything
remaining is the owner's.

**This handoff is deliberately short, and that is the session's main structural result.** State used
to live in twenty handoffs and two untracked blueprints. It now lives in three tracked files that a
fresh clone can open:

| Read this | For |
|---|---|
| **`docs/queue-of-record.md`** | Every track from Phase 2 and Phase 3, with a status. Guarded — a track cannot leave by being forgotten |
| **`docs/rt-answers-2026-09-13.md`** | Every settled ruling, including the eleven made this session |
| **`docs/prd-1..4-*.md`** | The complete PRD: use cases, feature scoring, functional requirements, screen specs |

**2127 tests in 174 files, green three runs. tsc clean. `origin/main == main == af196a4`.**

---

## What shipped

- **Phase 2 is complete.** Track M closed: the Taste Gem killed and published as `/method`'s sixth
  refusal.
- **Phase 3: N and R done; Q1 and Q2 done; S part-done by decision.** O and P wait on the owner; Q3
  is a one-way door that is his.
- **One name.** "The Taste Gym" → **Standard of Taste**, 54 occurrences, guarded.
- **One shell.** One container width and one header across `/`, `/learn`, `/method`, `/lab`,
  `/legal`. Two instrument flows joined it because their 11-point scale wrapped; two were measured
  and deliberately left narrow.
- **The product opens on the owner's frame** — being understood — in three beats, through the voice
  gate at `pointed` intensity.
- **Batch 3 of the copy commission applied**, including a factual correction to Hume's
  Sancho's-kinsmen anecdote that had been wrong on a live page.
- **The PRD, complete**, with guards: route coverage, ranking arithmetic, symbol citations, and
  completeness.
- **RT-Z2 ruled out** on the pipeline's own recovery sweep.

## Findings that must not be lost

1. **A ruling in an untracked file gets made twice.** RT-I was ruled 2026-09-01 and three handoffs
   carried it as open. This session nearly spent a second ruling on it.
2. **The audience claim is unevidenced and the only datum contradicts it.** The AI-music-producer
   positioning was chosen as "a positioning change with zero engine build". Asked directly, the one
   person who makes music this way said sound quality is not a problem he has. The README records it.
3. **A guard can fail for its own reasons and blame the document.** Two guards this session reported
   a defect that was entirely their own parse. Always read the failure before editing the subject.
4. **The transport still eats one level of backslash escaping.** Two more regexes were hollowed out
   this session and matched nothing while reporting success. Build patterns with
   `String.fromCharCode(92)`.
5. **I committed untested work once.** Three green runs, then an edit, then a commit. The pre-push
   hook caught it. Run the suite *after* the last edit, not before.
6. **A JSX comment in children position renders as page text.** It shipped to the viewport once and
   was a syntax error in a second place. `{/* */}` in children, `/* */` above `return (`.

## Open, in priority order — all of it the owner's

1. **Hand batches 4 and 5 to Cowork.** `docs/commission-batch-4.md` (the `/method` page) and
   `docs/commission-batch-5.md` (README, pitch page, the three front-door sentences). Both notes are
   written and their counts are pinned to the deck. This is the only remaining work that improves
   what a reviewer reads.
2. **Rule RT-P1's second half** — whether the preference instrument gets built. The mock is
   `docs/preference-mock-2026-09-13.md`. It is the only open product decision in the project.
3. **Track O** — four sittings, ~35 minutes, the only way a real number ever exists here. Optional
   under the owner's own reframe: no distribution, no expected outcome, the artifact is a record of
   process.
4. **Q3** — whether to track (and therefore publish) the two blueprints. One-way door; the repository
   is public.
5. **GitHub Support's reply.** Look for confirmation that cached views of the pre-rewrite history are
   purged; that sentence releases the four backup refs. Until then, **never
   `git push --force --mirror origin`.**
6. **"No sixth instrument"** — the last Track S item, deliberately unpublished until RT-P1 is ruled.

## Standing constraints

The repository is public. Tracking a file publishes it. Never `git push --force --mirror origin` —
four backup refs hold the last copies of two removed personal email addresses (one branch, three
tags). Push once per task; every push is a production deployment.
