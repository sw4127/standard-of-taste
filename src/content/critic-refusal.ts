/**
 * WHY THIS PRODUCT NEVER SCORES YOU AGAINST A CRITIC (E19/S14, PM ruling RT-X1 a).
 *
 * Cowork's batch-2 return found this argument written FOUR times, in four
 * wordings, across `/learn/comparison`, `/learn/methodology`,
 * `/learn/ranking-test` and the `/spread` frame. It declined to merge them,
 * correctly — merging is a structural change and that was a writing pass — and
 * asked for a ruling on which is canonical.
 *
 * ACROSS FOUR PAGES, FOUR WORDINGS ARE DEFENSIBLE: a reader meets one of them.
 * The reason they are not is that this is a refusal the product treats as
 * CONSTITUTIONAL. The Prestige Test measures how far a famous name moves a
 * listener's judgment; a second instrument that rewarded agreement with a
 * famous critic would contradict it on the same site. Four wordings are four
 * chances for one of them to soften into "how close you got", and the reader
 * who meets the softened one has been told something the product does not mean.
 *
 * `/learn/comparison` IS THE RECORD — it is the page whose subject this is, and
 * its wording was the one the writing pass left alone. The others state it by
 * citing this constant rather than by restating it.
 *
 * WHAT IS NOT IN HERE. Each page keeps its own MECHANISM prose — that only the
 * distance between two positions was ever imported, so agreement cannot be
 * computed afterwards even in principle. That is a fact about the data and it
 * reads differently on a reading-room page than on a pre-test frame. This
 * constant is the argument, not the paragraph.
 */

/**
 * The canonical sentence. Plain text, so it can sit inside any surrounding
 * markup; every page that makes this argument renders exactly this.
 */
export const CRITIC_CONTRADICTION =
  "Rewarding you for agreeing with a prestigious critic would have this product contradict " +
  "itself on the same screen.";
